namespace: production
instance: production-todo-service

replicaCount: 1

image:
  repository: ghcr.io/keremar/todo-app-todo-service
  tag: 5ca78aa-v1.0.0-prod

runtimeConfig:
  enabled: false

hotReloadRbac:
  enabled: false

database:
  storageClassName: longhorn-storageclass
  resources:
    requests:
      cpu: 50m
      memory: 128Mi
    limits:
      cpu: 250m
      memory: 256Mi

resources:
  requests:
    cpu: 50m
    memory: 128Mi
  limits:
    cpu: 250m
    memory: 256Mi
