namespace: staging
instance: staging-todo-service

replicaCount: 3

image:
  repository: ghcr.io/keremar/todo-app-todo-service
  tag: df1d130-v2.0-staging

runtimeConfig:
  enabled: true
  message: Staging config version 1 is active
  version: 2

hotReloadRbac:
  enabled: true

database:
  storageClassName: longhorn-storageclass
  resources:
    requests:
      cpu: 50m
      memory: 128Mi
    limits:
      cpu: 250m
      memory: 256Mi

resources:
  requests:
    cpu: 50m
    memory: 128Mi
  limits:
    cpu: 250m
    memory: 256Mi
