---
# Source: frontend/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: staging
  labels:
    app: frontend
    app.kubernetes.io/name: frontend
    app.kubernetes.io/instance: staging-frontend
    app.kubernetes.io/component: frontend
    app.kubernetes.io/managed-by: plain-manifests
spec:
  selector:
    app: frontend
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
  type: ClusterIP
