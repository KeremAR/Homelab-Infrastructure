# NGINX Gateway Fabric

This is the classic north-south Gateway API option, normally paired with
Calico and kube-proxy. It owns the `nginx` GatewayClass and the shared Gateway
in namespace `nginx-gateway`.

Follow [install-nginx.md](./install-nginx.md).
